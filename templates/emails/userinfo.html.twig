<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Hest-Test-Calendar</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: #4A90E2;
            color: white;
            text-align: center;
            padding: 15px;
            border-radius: 8px 8px 0 0;
        }
        .content {
            padding: 20px;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #888;
            margin-top: 20px;
        }
        .password {
            color: #E74C3C;
            font-weight: bold;
        }
        .btn {
            display: inline-block;
            padding: 10px 15px;
            color: white;
            background: #4A90E2;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 15px;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h2>Welcome to Hest-Test-Calendar!</h2>
    </div>
    <div class="content">
        <p>Hello <strong>{{ username }}</strong>,</p>
        <p>Your account has been successfully created.</p>
        <p><strong>Here are your login details:</strong></p>
        <ul style="background: #f3f3f3; padding: 10px; border-radius: 5px;">
            <li><strong>Email:</strong> {{ user_email }}</li> {# Changed from email to user_email #}
            <li><strong>Password:</strong> <span class="password">{{ password }}</span></li>
        </ul>
        <p>Please change your password after logging in.</p>
        <a href="{{ login_url }}" class="btn">Login Now</a>
    </div>
    <div class="footer">
        <p>Best Regards,</p>
        <p><strong>Hest-Test Support Team</strong></p>
    </div>
</div>
</body>
</html>
