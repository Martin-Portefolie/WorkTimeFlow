<div class="min-h-[40vh] p-4">
    {% if active|default('dashboard') == 'dashboard' %}
        {% include 'gui/admin/_overview.html.twig' %}
    {% else %}
        <section class="border border-zinc-800 bg-zinc-950 p-4">
            <div class="text-xs uppercase tracking-[0.3em] text-emerald-400">GUI CONTENT</div>
            <h2 class="mt-3 text-xl font-bold uppercase text-zinc-100">{{ active }}</h2>
            <p class="mt-2 text-zinc-500">
                Waiting for terminal sync.
            </p>
        </section>
    {% endif %}
</div>
