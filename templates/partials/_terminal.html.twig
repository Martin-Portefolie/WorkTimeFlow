{# Wrapper that routes to the correct terminal UI by role #}
{% if is_granted('ROLE_ADMIN') %}
    {% include 'partials/_admin.html.twig' %}
{% else %}
    {# TODO: style your user terminal later #}
    {% include 'partials/_profile.html.twig' %}
{% endif %}